import React from 'react'

const All = (props) => {
    return (
        <div style={{background:props.b}}>
            <p style={{color:props.c}}>The Word Is :{props.w}</p>
        </div>
    )
}

export default All
